<style>
  :root{
    --fbtc-navy:#082e52; --fbtc-cta:#ffcc00; --fbtc-text:#ffffff;
  }
  .fbtc-thanks{
    width:100vw; margin-left: calc(-50vw + 50%); position:relative; left:0;
    min-height: 100vh; display:flex; align-items:center; padding: 60px 0;
    background: linear-gradient(135deg, var(--fbtc-navy) 0%, #0b3a66 100%);
    font-family: "Roboto", sans-serif; overflow-x: hidden;
  }
  .fbtc-container{ max-width: 1100px; margin: 0 auto; padding: 0 20px; }
  
  .fbtc-thanks__card{
    background: rgba(255,255,255,0.1); border: 1px solid rgba(255,255,255,0.2);
    border-radius: 20px; padding: 40px; text-align: center;
    backdrop-filter: blur(10px); max-width: 700px; margin: 0 auto;
  }
  
  .fbtc-thanks__h1{ color: var(--fbtc-cta); font-size: 32px; margin-bottom: 20px; line-height: 1.2; }
  .fbtc-thanks__p{ color: #fff; font-size: 16px; line-height: 1.5; margin-bottom: 30px; }

  /* O SEGREDO: A seção de download começa escondida */
  #secao-download-pdf { display: none; margin-top: 20px; margin-bottom: 30px; }

  .fbtc-btn-download {
    background: var(--fbtc-cta); color: #082e52; padding: 15px 30px;
    font-weight: 900; text-decoration: none; border-radius: 50px;
    font-size: 18px; display: inline-block;
    box-shadow: 0 10px 20px rgba(0,0,0,0.2); transition: 0.3s;
  }
  .fbtc-btn-download:hover { transform: scale(1.05); background: #fff; }

  .fbtc-btn-whats {
    color: #fff; text-decoration: underline; font-size: 14px; margin-top: 20px; display: block;
  }
</style>

<section class="fbtc-thanks">
  <div class="fbtc-container">
    <div class="fbtc-thanks__card">
      
      <h1 class="fbtc-thanks__h1" id="titulo-obrigado">Obrigado! Recebemos seus dados.</h1>
      
      <p class="fbtc-thanks__p" id="texto-obrigado">
        Nossa equipe de coordenação entrará em contato em breve pelo WhatsApp para tirar suas dúvidas sobre o curso.
      </p>

      <div id="secao-download-pdf">
        <p style="color:#fff; font-weight:bold; margin-bottom:10px;">Clique abaixo para baixar seu arquivo:</p>
        
        <a href="LINK_DO_SEU_PDF_AQUI" target="_blank" class="fbtc-btn-download">
          📥 BAIXAR GRADE CURRICULAR
        </a>
      </div>

      <hr style="border:0; border-top:1px solid rgba(255,255,255,0.1); margin: 30px 0;">
      
      <p style="font-size:14px; color:rgba(255,255,255,0.7);">
        Precisa de urgência? 
        <a href="https://wa.me/5513997746343" target="_blank" style="color:#fff; font-weight:bold;">Chamar no WhatsApp agora</a>
      </p>
      
      <a href="/" class="fbtc-btn-whats">Voltar para o site</a>

    </div>
  </div>
</section>

<script>
  // SCRIPT INTELIGENTE
  // Ele verifica se na URL tem escrito "?tipo=grade"
  (function(){
    const params = new URLSearchParams(window.location.search);
    const tipo = params.get('tipo');

    // Se o tipo for "grade", mudamos a página para modo Download
    if(tipo === 'grade') {
      document.getElementById('titulo-obrigado').innerText = "Tudo pronto! Aqui está sua Grade.";
      document.getElementById('texto-obrigado').innerText = "Clique no botão abaixo para fazer o download do PDF completo com o conteúdo do curso.";
      
      // MOSTRA O BOTÃO
      document.getElementById('secao-download-pdf').style.display = 'block';
    }
  })();
</script>
